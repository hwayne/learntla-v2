<?xml version="1.0"?>
<root folder="termination">
<spec name="termination_1" num="3">
---- MODULE $name ----
EXTENDS Integers, Sequences, TLC


(* --algorithm counter
variable x = 0

define
  Inv == pc = "Done" => x = 3 
end define;

begin
  Loop:
    while x &lt; 3 do
      <s on="1">x := x + 1</s><s on="3">x := x + 1</s><s on="2">skip</s>;
    end while;
end algorithm; *)
====

</spec>
</root>
